<?php

/**
 * @file 
 * Contains tests for the BEM Markup module.
 */

/**
 * Test case.
 */
class BemMarkupTestCase extends DrupalWebTestCase {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('BEM Markup tests'),
      'description' => t('TODO: write me.'),
      'group' => t('BEM Markup'),
    );
  }

  /**
   * Implements setUp().
   */
  function setUp() {
    // Call the parent with an array of modules to enable for the test.
    parent::setUp(array('bem_markup'));

    // TODO: perform additional setup tasks here if required.
  }

  /**
   * Test the module's functionality.
   */
  function testTodoChangeThisName() {
    // TODO: write test!
  }

}